import React from 'react'

const Page = () => {
  return (
    <div
      className="grid grid-cols-4 divide-x max-w-6xl mx-auto min-h-screen h-screen size-full inset-0 z-50"
    >
      <span>{' '}</span>
      <span>{' '}</span>
      <span>{' '}</span>
      <span>{' '}</span>
    </div>
  )
}

export default Page